import { VisitorData } from "@/app/types";
import * as React from "react";

interface ContactCardProps {
  visitor: VisitorData;
}

const ContactCard: React.FC<ContactCardProps> = ({ visitor }) => {
  return <div></div>;
};
